package HashMaps;

import java.security.NoSuchAlgorithmException;
import java.util.LinkedList;

public class HashTable {
    LinkedList<LinkedList> list = new LinkedList(); // this is creating a list of lists
    public static void storeEmail(String Email, byte[] password) throws NoSuchAlgorithmException{
        //int index = (HashData.getHashValue(password));
    }
}
